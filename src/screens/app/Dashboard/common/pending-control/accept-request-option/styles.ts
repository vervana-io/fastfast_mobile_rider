import {StyleSheet} from 'react-native';
import {new_colors} from '../../../../../../new-colors';
import {wp} from '../../../../../../new-resources/config';

export const acceptRequestStyles = StyleSheet.create({
  container: {
    padding: 24,
    backgroundColor: new_colors.primary,
    gap: wp(20),
  },
});
